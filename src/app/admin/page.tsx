import { requireRole } from "@/lib/auth/session";
export const dynamic = "force-dynamic";
export default async function Admin(){const { supabase } = await requireRole("admin"); const [{ count: total }, { count: active }, { count: delivered }, { count: incidents }] = await Promise.all([supabase.from("orders").select("id", { count: "exact", head: true }),supabase.from("orders").select("id", { count: "exact", head: true }).in("status", ["assigned","heading_to_pickup","at_pickup","picked_up","heading_to_delivery","at_delivery"]),supabase.from("orders").select("id", { count: "exact", head: true }).eq("status", "delivered"),supabase.from("orders").select("id", { count: "exact", head: true }).eq("status", "incident")]); const metrics=[["Pedidos",total ?? 0],["Activos",active ?? 0],["Completados",delivered ?? 0],["Incidentes",incidents ?? 0]]; return <main className="mx-auto max-w-6xl px-4 py-8"><p className="font-bold text-yellow-400">ADMINISTRACIÓN</p><h1 className="mt-2 text-3xl font-bold">Panel general</h1><section className="mt-8 grid grid-cols-2 gap-3 md:grid-cols-4">{metrics.map(([label,value])=><article className="rounded-xl bg-zinc-900 p-4" key={String(label)}><p className="text-sm text-zinc-400">{label}</p><p className="mt-3 text-xl font-bold">{value}</p></article>)}</section><section className="mt-8 rounded-xl border border-white/10 p-6"><h2 className="font-bold">Operación segura</h2><p className="mt-2 text-sm text-zinc-400">Las consultas se ejecutan con RLS y los cambios de estado pasan por una función auditada.</p></section></main>}
