"use client";
import { useState } from "react";
import { createSupabaseBrowserClient } from "@/lib/supabase/client";
export default function ForgotPassword(){const [message,setMessage]=useState(""); const submit=async(form:FormData)=>{const supabase=createSupabaseBrowserClient(); const { error }=await supabase.auth.resetPasswordForEmail(String(form.get("email")),{redirectTo:`${window.location.origin}/auth/reset-password`}); setMessage(error?error.message:"Si existe una cuenta, te enviamos un enlace para restablecerla.");}; return <main className="mx-auto max-w-md px-4 py-20"><h1 className="text-3xl font-bold">Restablecer contraseña</h1><form action={submit} className="mt-7 grid gap-3"><input required name="email" type="email" placeholder="Correo" className="rounded-lg bg-zinc-900 px-4 py-3"/><button className="rounded-lg bg-yellow-400 py-3 font-bold text-black">Enviar enlace</button></form>{message&&<p role="status" className="mt-4 text-sm text-zinc-300">{message}</p>}</main>}
