"use client";

import { Award, ChevronRight, Clock3, PackageCheck, Sparkles, Star, Trophy } from "lucide-react";
import { useState } from "react";
import type { ProfileExperienceData } from "@/lib/profiles/experience";

const roleLabel = { customer: "Cliente", courier: "Cadete", admin: "Administrador" };
const statusLabel: Record<string, string> = { delivered: "Entregado", confirmed: "Disponible", assigned: "Asignado", heading_to_pickup: "En retiro", at_pickup: "En retiro", picked_up: "Retirado", heading_to_delivery: "En camino", at_delivery: "En destino", incident: "Incidencia", pending_confirmation: "Pendiente", cancelled: "Cancelado" };

export function ProfileExperience({ name, email, data }: { name: string; email?: string | null; data: ProfileExperienceData }) {
  const [tab, setTab] = useState<"summary" | "activity" | "achievements">("summary");
  const progress = data.nextLevelPoints ? Math.min(100, Math.round((data.points / data.nextLevelPoints) * 100)) : 100;
  const stars = Math.max(1, Math.min(5, Math.ceil(data.total / 3)));

  return <section className="mt-7 overflow-hidden rounded-2xl border border-brand/20 bg-zinc-900">
    <div className="bg-gradient-to-br from-brand/20 via-zinc-900 to-zinc-900 p-4 sm:p-6">
      <div className="flex flex-wrap items-start justify-between gap-4">
        <div className="flex min-w-0 gap-4"><div className="grid size-14 shrink-0 place-items-center rounded-2xl bg-brand text-xl font-black text-brand-foreground">{name.slice(0, 1).toUpperCase()}</div><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-wider text-brand">Perfil {roleLabel[data.role]}</p><h1 className="mt-1 break-words text-2xl font-bold">{name}</h1>{email && <p className="mt-1 break-all text-sm text-zinc-400">{email}</p>}</div></div>
        <div className="shrink-0 rounded-xl border border-amber-300/25 bg-amber-300/10 px-4 py-3 text-right"><p className="flex justify-end gap-1 text-amber-300">{Array.from({ length: 5 }, (_, index) => <Star key={index} className={`size-4 ${index < stars ? "fill-current" : "opacity-25"}`}/>)}</p><p className="mt-1 text-sm font-bold">{data.level}</p></div>
      </div>
      <div className="mt-6 grid gap-3 sm:grid-cols-4"><Metric label="Pedidos" value={data.total} icon={<PackageCheck className="size-4"/>}/><Metric label="Completados" value={data.completed} icon={<Trophy className="size-4"/>}/><Metric label="En curso" value={data.active} icon={<Clock3 className="size-4"/>}/><Metric label={data.highlightLabel} value={data.highlightValue} icon={<Sparkles className="size-4"/>}/></div>
    </div>
    <div className="border-y border-white/10 px-4"><div className="flex gap-5 overflow-x-auto"><Tab active={tab === "summary"} onClick={() => setTab("summary")}>Resumen</Tab><Tab active={tab === "activity"} onClick={() => setTab("activity")}>Actividad</Tab><Tab active={tab === "achievements"} onClick={() => setTab("achievements")}>Logros</Tab></div></div>
    <div className="p-4 sm:p-6">{tab === "summary" && <div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div><p className="flex items-center gap-2 font-bold"><Award className="size-5 shrink-0 text-brand"/>Tu progreso</p><p className="mt-2 text-sm text-zinc-400">Sumás puntos con cada operación completada. El nivel refleja tu actividad real en la plataforma.</p><div className="mt-5 rounded-xl bg-black/20 p-4"><div className="flex justify-between gap-3 text-sm"><span className="font-bold">{data.points.toLocaleString("es-AR")} puntos</span><span className="text-right text-zinc-400">{data.nextLevel}</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-zinc-700"><div className="h-full rounded-full bg-brand" style={{ width: `${progress}%` }}/></div><p className="mt-2 text-xs text-zinc-400">{data.nextLevelPoints > data.points ? `${data.nextLevelPoints - data.points} puntos para el próximo nivel.` : "Llegaste al nivel máximo."}</p></div></div><RecentActivity items={data.recent}/></div>}{tab === "activity" && <RecentActivity items={data.recent} expanded/>}{tab === "achievements" && <div className="grid gap-3 sm:grid-cols-3"><Achievement active={data.total >= 1} title="Primer paso" text="Registraste tu primera operación."/><Achievement active={data.completed >= 3} title="Constancia" text="Completaste 3 operaciones."/><Achievement active={data.completed >= 10} title="Nivel experto" text="Completaste 10 operaciones."/></div>}</div>
  </section>;
}

function Metric({ label, value, icon }: { label: string; value: string | number; icon: React.ReactNode }) { return <div className="rounded-xl border border-white/10 bg-black/15 p-3"><p className="flex items-center gap-2 text-xs text-zinc-400">{icon}{label}</p><p className="mt-2 text-xl font-bold capitalize">{value}</p></div>; }
function Tab({ active, onClick, children }: { active: boolean; onClick: () => void; children: React.ReactNode }) { return <button onClick={onClick} className={`border-b-2 px-1 py-4 text-sm font-semibold ${active ? "border-brand text-brand" : "border-transparent text-zinc-400"}`}>{children}</button>; }
function Achievement({ active, title, text }: { active: boolean; title: string; text: string }) { return <article className={`rounded-xl border p-4 ${active ? "border-brand/35 bg-brand/5" : "border-white/10 opacity-50"}`}><Trophy className="size-5 text-brand"/><h2 className="mt-3 font-bold">{title}</h2><p className="mt-1 text-sm text-zinc-400">{text}</p></article>; }
function RecentActivity({ items, expanded = false }: { items: ProfileExperienceData["recent"]; expanded?: boolean }) { const visible = expanded ? items : items.slice(0, 3); return <div><p className="font-bold">Actividad reciente</p><div className="mt-3 divide-y divide-white/10 rounded-xl border border-white/10">{visible.length ? visible.map((item) => <article className="flex min-w-0 items-center justify-between gap-3 p-3 text-sm" key={item.code}><div className="min-w-0"><p className="break-words font-semibold">{item.code}</p><p className="mt-1 text-xs text-zinc-500">{new Date(item.createdAt).toLocaleDateString("es-AR")}</p></div><div className="shrink-0 text-right"><p className="text-xs text-brand">{statusLabel[item.status] ?? item.status}</p>{item.amount !== null && <p className="mt-1 text-xs text-zinc-400">${Number(item.amount).toLocaleString("es-AR")}</p>}</div><ChevronRight className="size-4 shrink-0 text-zinc-500"/></article>) : <p className="p-4 text-sm text-zinc-400">Aún no hay actividad registrada.</p>}</div></div>; }
